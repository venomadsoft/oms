'use strict';

angular.module('omsApp')
    .factory('Note', function ($resource, DateUtils) {
        return $resource('api/notes/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
